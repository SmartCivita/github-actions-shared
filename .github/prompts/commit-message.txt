Create a SHORT conventional commit message. Output ONLY the message, nothing else.

CURRENT MESSAGE: __CURRENT_MSG__
CHANGED FILES (__FILE_COUNT__):
__CHANGED_FILES__

CODE DIFF (added lines only):
__DIFF_CONTENT__

RULES:
- Format: type(scope) description
- Max 72 characters
- Types: feat, fix, docs, style, refactor, test, chore, perf, ci, build, revert
- Description must reflect the actual code changes
- NEVER use backticks, code fences, or markdown
- Output ONLY the commit message on a single line
